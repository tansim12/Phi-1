#include <stdio.h>
int main()
{
    int x;
    int y;

    scanf("%d  %d", &x, &y);

    if (x > y)
    {
        /* code */
        printf("value of x = %d", x);
    }
    else
    {
        printf("value of y = %d", y);
    }
  

    

    return 0;
}